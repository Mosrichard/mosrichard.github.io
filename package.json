{
  "name": "mos-richard-portfolio",
  "version": "1.0.0",
  "homepage": "https://mosrichard.github.io/MOS-PORTFOLIO",
  "private": true,
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "gsap": "^3.14.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--max_old_space_size=4096 && react-scripts start",
    "build": "set NODE_OPTIONS=--max_old_space_size=4096 && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
